#include <stdio.h>
#include <stdlib.h>
#define MAX 100
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	//char a[MAX]="Nguyen Van An";
	char a[MAX]={'N','g','u','y','e','n'};
	
	//scanf ("%10s",a);
	//scanf ("%[^\n]",a);
	//gets(a);
	
	//printf ("%s",a);
	puts(a);
	return 0;
}
